@model Strona_do_rezerwacji_biletów.Models.Event

@{
    ViewData["Title"] = "Event Details";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h1 class="card-title text-center">@Model.Title</h1>
                    <hr>
                    <p class="card-text">
                        <strong>Date:</strong> @Model.Date.ToString("yyyy-MM-dd HH:mm")
                    </p>
                    <p class="card-text">
                        <strong>Description:</strong> @Model.Description
                    </p>
                    <p class="card-text">
                        <strong>Category:</strong> @Model.Category
                    </p>
                    <p class="card-text">
                        <strong>Available Normal Seats:</strong>
                        @if (Model.AvailableNormalSeats > 0)
                        {
                            <span class="text-success">@Model.AvailableNormalSeats</span>
                        }
                        else
                        {
                            <span class="text-danger">No seats available</span>
                        }
                    </p>
                    <p class="card-text">
                        <strong>Available VIP Seats:</strong>
                        @if (Model.AvailableVIPSeats > 0)
                        {
                            <span class="text-success">@Model.AvailableVIPSeats</span>
                        }
                        else
                        {
                            <span class="text-danger">No VIP seats available</span>
                        }
                    </p>
                    <div class="d-flex justify-content-between mt-4">
                        <a href="@Url.Action("Index", "Events")" class="btn btn-secondary">
                            Back to Events
                        </a>
                        @if (Model.AvailableNormalSeats > 0 || Model.AvailableVIPSeats > 0)
                        {
                            <form asp-action="Reserve" asp-route-id="@Model.Id" method="get">
                                <button type="submit" class="btn btn-primary">Reserve Now</button>
                            </form>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
